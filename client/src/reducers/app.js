const initialState = {
}

/**
 * App reducer.
 */
export const appReducer = (state = initialState, action) => {
    switch (action.type) {
        // Depending on the action type.
        default:
            return {...state}
    }
}